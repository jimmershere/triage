# Pinned versions are coordinated with CI's pip-audit check. When bumping,
# verify advisories against the lockstep set:
#   fastapi -> starlette (transitive)
#   python-multipart (consumed via /ingest UploadFile)
fastapi==0.118.2
starlette==0.47.2
uvicorn==0.30.6
pika==1.3.2
python-dotenv==1.2.2
pydantic==2.9.0
python-multipart==0.0.31
psycopg2-binary==2.9.9
ldap3==2.9.1
cryptography>=42,<46
