# Containerized local deployment uses SCRAM over the Docker network.
# Reintroduce hostssl rules when managed TLS is enabled.
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             all                     scram-sha-256
