# Custom PostgreSQL configuration for containerized local deployment.
# TLS is disabled here because bind-mounted key ownership is not stable across hosts.
# Re-enable with managed cert provisioning when moving beyond local Docker deployment.
listen_addresses = '*'
ssl = off
password_encryption = 'scram-sha-256'
# Rely on defaults for other settings
